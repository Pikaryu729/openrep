# Optional. The UI talks to /api on its own origin by default: `pnpm dev`
# proxies /api to the backend, and the packaged app serves both from one
# process. Set this only to point the dev UI at a backend on another origin —
# note it must include the /api suffix.
# VITE_API_BASE_URL=http://localhost:8765/api
