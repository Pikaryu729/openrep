node_modules/
.claude/settings.local.json

# Local SQLite database (local-first: each user's data lives on their machine)
*.db
*.db-journal

# Per-worktree dev state written by scripts/dev.sh: the resolved ports and,
# in a linked worktree, that worktree's own scratch database.
.dev/

.env
.DS_Store

# Build artifacts
backend/dist/
backend/openrep/static/
*.egg-info/



.claude/worktrees
